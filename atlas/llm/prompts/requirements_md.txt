You are an expert Principal Systems Architect. Your task is to take a structured JSON specification file and compile it into a comprehensive, production-grade `requirements.md` file. 
    
    This document will serve as the absolute Single Source of Truth (SSoT) for the entire development lifecycle.
    
    ### Guidelines for Generation:
    1. **No Conversational Prose:** Start directly with the `#` Title. Do not include markdown code ticks (```), wrappers, introduction lines ("Here is your requirements file..."), or sign-offs.
    2. **Be Highly Technical and Precise:** Do not use vague language. Translate fields, endpoints, and roles into concrete, implementation-ready descriptions.
    3. **Traceability:** Ensure every entity field and API endpoint from the JSON maps to an explicit functional requirement block.
    
    --- STRUCTURAL JSON INPUT ---
    {project_specs}
    
    --- EXPECTED MARKDOWN OUTLINE DOCUMENT STRUCTURE ---
    
    # Software Requirements Specification (SRS): [Insert project_name here]
    
    ## 1. Executive Summary & Project Type
    * **Project Name:** [project_name]
    * **Project Type:** [project_type]
    * **Target Architecture:** Decoupled Client-Server Core
    * **Technical Stack:** Frontend: `[frontend]` | Backend: `[backend]` | Database: `[database]` | Authentication: `[authentication]`
    
    ---
    
    ## 2. System Roles & Access Control
    *For every role listed in the JSON roles array, define their explicit system boundaries:*
    * **[Role Name]:** Detailed explanation of data boundaries, access limitations, and target interactions.
    
    ---
    
    ## 3. Data Model & Database Schema Specifications
    *Iterate through the JSON 'entities' array. For each entity, generate a clean markdown schema section:*
    
    ### 3.x Entity: [Entity Name]
    * **Description:** Purpose of this table/collection.
    * **Database Fields:**
    | Field Name | Data Type | Attributes / Constraints | Operational Purpose |
    | :--- | :--- | :--- | :--- |
    | [field_name] | [type] | [e.g., unique=True, max_length=150, blank=True] | Explanation |
    
    * **Relationships:**
      * `[Relationship Type]` to `[Target Entity]` (On Delete: `[on_delete]`, Related Name: `[related_name]`).
    
    ---
    
    ## 4. API Interface & Routing Specifications
    *Iterate through the JSON 'required_api_endpoints' array. For every endpoint object, create a highly technical specification card:*
    
    ### 4.x `[HTTP Method]` `[Path]`
    * **Internal Framework Action:** `[action]` ViewSet method link.
    * **Authentication Required:** `[True/False]` (Enforced via `[authentication]`).
    * **Functional Objective:** What business logic occurs when this network hook fires.
    * **Expected Input / Payload:** (List keys or URL parameters required).
    * **Success Criteria:** HTTP status code expected on success.
    
    ---
    
    ## 5. Non-Functional & Security Requirements
    * **Data Security & Stateful Isolation:** Detail how `[authentication]` tokens must be transmitted (e.g., HTTPOnly Cookies or Authorization Headers) and how the `[database]` isolates data per user.
    * **Database Integrity:** Rules around cascade constraints and performance indexes.
    
    Now, compile the `requirements.md` source code matching this layout exactly based on the structural JSON context provided above.