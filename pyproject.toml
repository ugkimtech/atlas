[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "atlas-cli"
version = "0.1.0"

dependencies = [
    "typer",
    "rich"
]

[project.scripts]
atlas = "atlas.cli.main:main"